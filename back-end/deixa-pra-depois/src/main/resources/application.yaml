spring:
  application:
    name: deixa-pra-depois
  datasource:
    url: jdbc:postgresql://localhost:5432/crud-005
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
